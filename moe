这部分需要把 Figure 3 与 Table 1/Figure 4 分开看：

- Figure 3：研究现实中如何选择最优 \(\gamma\)，所以使用不同的 \(c>0\)。
- Table 1、Figure 4：研究算法本身的“速度—运算量”权衡，所以暂时假设 \(c=\hat c=0\)。

## 一、为什么 Table 1 和 Figure 4 假设 \(c=\hat c=0\)？

两个参数含义不同：

\[
c=\frac{\text{小模型单步耗时}}{\text{目标模型单步耗时}}
\]

\[
\hat c=\frac{\text{小模型单步运算量}}{\text{目标模型单步运算量}}
\]

令：

\[
c=\hat c=0
\]

表示假设草稿模型 \(M_q\) 的时间和运算成本可以忽略。

这不是说真实的小模型一定没有成本，而是为了排除小模型自身的影响，单独观察：

- \(\alpha\)：候选接受率；
- \(\gamma\)：候选数量；

如何影响速度和目标模型的无效计算。

这种假设也对应 n-gram、查表或简单启发式模型等成本近似为零的草稿模型。

### 公式会变得非常清晰

定理 3.8 的加速比是：

\[
S=
\frac{1-\alpha^{\gamma+1}}
{(1-\alpha)(1+\gamma c)}
\]

令 \(c=0\)：

\[
\boxed{
S=
\frac{1-\alpha^{\gamma+1}}{1-\alpha}
=E(N)
}
\]

即加速比直接等于每轮平均生成的 token 数。

定理 3.11 的运算量增加系数是：

\[
O=
\frac{(1-\alpha)(\gamma\hat c+\gamma+1)}
{1-\alpha^{\gamma+1}}
\]

令 \(\hat c=0\)：

\[
\boxed{
O=
\frac{(1-\alpha)(\gamma+1)}
{1-\alpha^{\gamma+1}}
=
\frac{\gamma+1}{E(N)}
}
\]

虽然忽略了小模型运算量，目标模型仍计算了 \(\gamma+1\) 个位置。因此，如果候选被拒绝，仍会有目标模型计算被浪费。

## 二、Table 1 怎么看？

例如表中的：

\[
\alpha=0.8,\qquad\gamma=5
\]

每轮平均生成：

\[
E(N)=
\frac{1-0.8^6}{1-0.8}
\approx3.69
\]

因为 \(c=0\)，所以速度提升为：

\[
S=3.69\times
\]

目标模型每轮实际计算了：

\[
\gamma+1=6
\]

个位置，但平均只得到 \(3.69\) 个有效 token，所以运算量增加系数为：

\[
O=\frac6{3.69}\approx1.63\times
\]

这说明：

> 通过并行计算获得了 \(3.69\times\) 的速度，但执行了普通解码约 \(1.63\times\) 的算术运算。

## 三、Figure 4 怎么看？

Figure 4 的横轴是平均接受率 \(\alpha\)，不同颜色表示不同的 \(\gamma\)。

- 实线 `Speed`：walltime 加速比；
- 虚线 `Ops`：总算术运算量增加倍数。

### 随着 \(\alpha\) 增大

实线上升，因为更多候选被接受，每轮产生更多 token。

虚线下降，因为更少的目标模型计算被丢弃。

因此高接受率同时意味着：

\[
\text{速度更快，而且计算浪费更少}
\]

### 随着 \(\gamma\) 增大

潜在加速比提高，因为一轮最多可以生成 \(\gamma+1\) 个 token。

但是在 \(\alpha\) 较低时，大 \(\gamma\) 会产生大量浪费。例如前几个 token 就被拒绝，后面位置虽然已经计算，却不能使用。

因此 Figure 4 展示的核心权衡是：

\[
\boxed{
\gamma\text{ 大}
\Rightarrow
\text{潜在速度更高，但低接受率时浪费更多}
}
\]

## 四、Figure 3 怎么看？

Figure 3 用于选择能够最大化 walltime 加速比的 \(\gamma\)：

\[
\boxed{
\gamma^*
=
\arg\max_{\gamma\in\mathbb Z^+}
\frac{1-\alpha^{\gamma+1}}
{(1-\alpha)(1+\gamma c)}
}
\]

图中：

- 横轴：平均接受率 \(\alpha\)；
- 纵轴：最优候选数 \(\gamma^*\)；
- 不同曲线：不同的小模型成本 \(c\)。

### 规律一：\(\alpha\) 越高，最优 \(\gamma\) 越大

如果候选很容易被接受，多生成一些候选通常是有价值的：

\[
\alpha\uparrow\quad\Rightarrow\quad\gamma^*\uparrow
\]

例如当 \(\alpha=0.8\) 时：

- \(c=0.01\)：最优 \(\gamma\) 约为 14；
- \(c=0.05\)：最优 \(\gamma\) 约为 8；
- \(c=0.1\)：最优 \(\gamma\) 约为 6。

### 规律二：\(c\) 越高，最优 \(\gamma\) 越小

小模型生成每个候选都需要成本。如果 \(M_q\) 较慢，生成太多草稿 token 会抵消加速收益：

\[
c\uparrow\quad\Rightarrow\quad\gamma^*\downarrow
\]

反过来，如果小模型几乎没有成本，就可以选择更大的 \(\gamma\)。

### 为什么曲线呈阶梯状？

因为：

\[
\gamma\in\{1,2,3,\ldots\}
\]

只能取整数。随着 \(\alpha\) 连续变化，最优值会从 5 突然跳到 6，而不会出现 \(5.4\)，所以图线呈阶梯形。

## 三张图表的整体关系

\[
\boxed{
\begin{aligned}
\text{Figure 3：}&\quad
给定真实的 }\alpha,c\text{，选择速度最优的 }\gamma\\
\text{Table 1：}&\quad
列出若干 }\alpha,\gamma\text{ 组合的具体数值}\\
\text{Figure 4：}&\quad
展示速度提升与总运算量增加之间的权衡
\end{aligned}
}
\]

需要注意，Figure 3 只以 walltime 最大化为目标。如果还需要限制显存、能耗或总算力，实际选择的 \(\gamma\) 可能小于图中的最优值。
