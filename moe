论文中的 Speculative Decoding（推测解码）可以概括为：

> 先让“小模型快速起草多个 token”，再让“大模型一次性并行审核，从而减少大模型串行调用次数。

论文记号：

- \(M_p\)：目标模型（Target Model），生成质量高，但推理慢。
- \(M_q\)：近似模型（Draft/Approximation Model），规模较小，速度快。
- \(p(x_t\mid x_{<t})\)、\(q(x_t\mid x_{<t})\)：两个模型对下一个 token 的概率分布。
- \(\gamma\)：小模型每轮预先生成的 token 数量。

### 传统自回归解码

普通解码每次只能生成一个 token：

\[
x_1 \rightarrow x_2 \rightarrow x_3 \rightarrow \cdots
\]

每生成一个 token，都必须串行运行一次大模型。因此生成 \(N\) 个 token，通常需要大模型连续运行 \(N\) 次。

### Speculative Decoding 的一轮流程

假设 \(\gamma=4\)。

1. 小模型快速起草

小模型 \(M_q\) 自回归地产生 4 个候选 token：

\[
x_1,x_2,x_3,x_4
\]

由于小模型便宜，这部分计算成本较低。

2. 大模型一次性审核

目标模型 \(M_p\) 对下面这些位置的概率分布进行并行计算：

\[
p_1,p_2,p_3,p_4,p_5
\]

其中：

- \(p_1\)：根据原始前缀预测 \(x_1\)
- \(p_2\)：根据“前缀+\(x_1\)”预测 \(x_2\)
- \(p_3\)：根据“前缀+\(x_1,x_2\)”预测 \(x_3\)
- 依此类推

Transformer 可以在一次前向传播中计算这些位置，因此不需要把大模型串行运行 5 次。

3. 依次接受候选 token

对于小模型生成的候选 \(x_i\)，其接受概率为：

\[
\min\left(1,\frac{p_i(x_i)}{q_i(x_i)}\right)
\]

直观地说：

- 如果大模型认为该 token 至少和小模型一样合理，就直接接受。
- 如果小模型过度高估了该 token，就只以 \(p_i(x_i)/q_i(x_i)\) 的概率接受。

候选必须从左到右连续接受。一旦某个 token 被拒绝，后面的候选也全部丢弃，因为它们建立在错误前缀之上。

4. 补充一个由大模型决定的 token

- 如果某个候选被拒绝，就从修正分布

\[
p'(x)=\operatorname{norm}\big(\max(0,p(x)-q(x))\big)
\]

重新采样一个 token，替换第一个被拒绝的候选。

- 如果全部 \(\gamma\) 个候选都被接受，则利用大模型已经算出的 \(p_{\gamma+1}\)，再生成一个额外 token。

因此，一轮大模型调用一定会产生至少 1 个 token，最多产生：

\[
\gamma+1
\]

个 token。

### 举例

小模型一次起草：

> “今天天气非常适合跑步”

假设大模型依次接受：

> “今天” → “天气” → “非常” → “适合”

但拒绝“跑步”，那么本轮输出可能是：

> “今天天气非常适合散步”

其中前四个 token 来自小模型，最后一个修正 token 来自大模型的修正分布。

### 最关键的性质

Speculative Decoding 并不是简单地“相信小模型”。它通过接受概率和修正分布保证：

\[
\text{最终输出分布}
=
\text{仅使用目标模型时的输出分布}
\]

所以理论上：

- 不改变目标模型的生成结果分布；
- 不需要重新训练目标模型；
- 最坏情况下每轮仍然生成一个 token，不会比普通串行解码需要更多的大模型串行轮次；
- 当小模型与大模型预测接近时，一次大模型调用可以确认多个 token，从而显著降低延迟。

论文 Figure 1 的例子中，38 个 token 只需要目标模型串行运行 9 次；论文报告在部分 T5-XXL 实验中取得了约 \(2\text{–}3\times\) 的延迟改善。来源：:codex-file-citation{path="/home/hesai/推测解码/output/papers/01_Fast_Inference_from_Transformers_via_Speculative_Decoding.pdf" purpose="source"}

- :codex-followup[图解完整流程]{prompt="请用流程图和具体 token 示例进一步解释 speculative decoding"}
- :codex-followup[推导分布不变性]{prompt="请推导 speculative sampling 为什么能够保证输出分布与目标模型完全相同"}
- :codex-followup[逐行解析算法]{prompt="请逐行讲解论文中的 Algorithm 1，并解释每个公式"}
