定理 3.11 分析的不是 walltime，而是：

> Speculative decoding 相对于普通解码，平均会执行多少倍的算术运算。

结论是：

\[
\boxed{
\text{Operation Increase}
=
\frac{(1-\alpha)(\gamma\hat c+\gamma+1)}
{1-\alpha^{\gamma+1}}
}
\]

其中：

- \(\alpha\)：平均接受率；
- \(\gamma\)：每轮草稿 token 数；
- \(\hat c\)：小模型生成一个 token 的运算量与目标模型生成一个 token 的运算量之比。

## 1. 定义普通解码的运算量

设目标模型生成一个 token 所需的算术运算量为：

\[
\hat T
\]

那么普通自回归解码每生成一个 token 的运算量就是：

\[
C_{\text{normal}}=\hat T
\]

Definition 3.10 定义：

\[
\hat c=
\frac{\text{小模型单 token 运算量}}
{\text{目标模型单 token 运算量}}
\]

因此，小模型生成一个 token 的运算量为：

\[
\hat c\hat T
\]

注意：

- \(c\) 是运行时间之比；
- \(\hat c\) 是算术运算量之比。

二者不一定相等。

## 2. 一轮 Algorithm 1 的运算量

### 小模型部分

小模型串行生成 \(\gamma\) 个草稿 token，因此运算量为：

\[
C_q=\gamma\hat c\hat T
\]

### 目标模型部分

目标模型需要计算：

\[
p_1,p_2,\ldots,p_{\gamma+1}
\]

虽然这些位置可以并行计算，所以 walltime 可以近似一次调用，但每个位置的算术运算依然真实存在。

目标模型部分的总运算量为：

\[
C_p=(\gamma+1)\hat T
\]

因此，一轮 Algorithm 1 的总运算量为：

\[
\begin{aligned}
C_{\text{iteration}}
&=C_q+C_p\\
&=\gamma\hat c\hat T+(\gamma+1)\hat T\\
&=(\gamma\hat c+\gamma+1)\hat T
\end{aligned}
\]

## 3. 一轮平均生成多少 token？

根据 3.1：

\[
E(N)
=
\frac{1-\alpha^{\gamma+1}}{1-\alpha}
\]

所以 speculative decoding 平均生成一个 token 的运算量为：

\[
\begin{aligned}
C_{\text{spec/token}}
&=
\frac{C_{\text{iteration}}}{E(N)}\\
&=
\frac{(\gamma\hat c+\gamma+1)\hat T}
{\dfrac{1-\alpha^{\gamma+1}}{1-\alpha}}\\
&=
\frac{(1-\alpha)(\gamma\hat c+\gamma+1)}
{1-\alpha^{\gamma+1}}\hat T
\end{aligned}
\]

## 4. 与普通解码比较

普通解码每个 token 的运算量是 \(\hat T\)，所以运算量增加系数为：

\[
\begin{aligned}
\frac{C_{\text{spec/token}}}{C_{\text{normal}}}
&=
\frac{
\dfrac{(1-\alpha)(\gamma\hat c+\gamma+1)}
{1-\alpha^{\gamma+1}}\hat T
}{\hat T}\\
&=
\boxed{
\frac{(1-\alpha)(\gamma\hat c+\gamma+1)}
{1-\alpha^{\gamma+1}}
}
\end{aligned}
\]

这就是定理 3.11。

## 为什么加速了，运算量反而可能增加？

关键区别是“并行时间”和“总计算量”：

- 定理 3.8 计算 walltime：\(\gamma+1\) 个位置并行，因此目标模型时间近似记为 \(T\)；
- 定理 3.11 计算总运算量：即使并行执行，\(\gamma+1\) 个位置仍然分别做了计算，因此记为 \((\gamma+1)\hat T\)。

如果某个草稿 token 被拒绝，其后所有位置的目标模型计算都会被丢弃。这些计算缩短了并行延迟，但没有产生有效 token，因而增加总运算量。

例如：

\[
\alpha=0.8,\qquad\gamma=4,\qquad\hat c=0.05
\]

运算量增加系数为：

\[
\frac{(1-0.8)(4\times0.05+4+1)}
{1-0.8^5}
\approx1.55
\]

也就是说：

- 总算术运算量约增加到普通解码的 \(1.55\) 倍；
- 但由于这些运算高度并行，walltime 仍然可能明显下降。

因此 speculative decoding 本质上是：

\[
\boxed{\text{用更多并行计算量，换取更少的串行等待时间}}
\]
