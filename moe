定理 3.8 推导的是 speculative decoding 相对于普通自回归解码的期望 walltime 加速比。

结论为：

\[
\boxed{
\text{Speedup}
=
\frac{1-\alpha^{\gamma+1}}
{(1-\alpha)(\gamma c+1)}
}
\]

推导分三步。

## 1. 普通解码的单位 token 耗时

设目标模型 \(M_p\) 生成一个 token 所需时间为：

\[
T
\]

因此普通自回归解码的单位 token 成本是：

\[
C_{\text{normal}}=T
\]

## 2. Algorithm 1 每轮的耗时

成本系数定义为：

\[
c=\frac{T_q}{T_p}
\]

所以小模型运行一次的时间是：

\[
T_q=cT
\]

为了提出 \(\gamma\) 个候选，小模型需要自回归运行 \(\gamma\) 次：

\[
T_{\text{draft}}=\gamma cT
\]

目标模型对所有候选进行一次并行验证。论文假设计算资源足够，使这次并行验证耗时约为：

\[
T_{\text{target}}=T
\]

所以一轮 Algorithm 1 的总耗时为：

\[
\begin{aligned}
T_{\text{iteration}}
&=\gamma cT+T\\
&=(\gamma c+1)T
\end{aligned}
\]

## 3. 每轮平均生成的 token 数

根据 3.1：

\[
E(N)
=
\frac{1-\alpha^{\gamma+1}}{1-\alpha}
\]

所以 speculative decoding 生成一个 token 的平均时间为：

\[
\begin{aligned}
C_{\text{spec}}
&=
\frac{\text{每轮耗时}}
{\text{每轮平均生成 token 数}}\\
&=
\frac{(\gamma c+1)T}
{\dfrac{1-\alpha^{\gamma+1}}{1-\alpha}}\\
&=
\frac{(\gamma c+1)(1-\alpha)}
{1-\alpha^{\gamma+1}}T
\end{aligned}
\]

## 4. 计算加速比

加速比定义为：

\[
\text{Speedup}
=
\frac{\text{普通解码单位 token 耗时}}
{\text{推测解码单位 token 耗时}}
\]

代入：

\[
\begin{aligned}
\text{Speedup}
&=
\frac{T}
{\dfrac{(\gamma c+1)(1-\alpha)}
{1-\alpha^{\gamma+1}}T}\\
&=
\boxed{
\frac{1-\alpha^{\gamma+1}}
{(1-\alpha)(\gamma c+1)}
}
\end{aligned}
\]

也可以写成更直观的形式：

\[
\boxed{
\text{Speedup}
=
\frac{\text{每轮平均生成 token 数}}
{\text{每轮相对时间成本}}
=
\frac{E(N)}{1+\gamma c}
}
\]

### 数值例子

假设：

\[
\alpha=0.8,\qquad \gamma=4,\qquad c=0.05
\]

每轮平均生成：

\[
E(N)
=
\frac{1-0.8^5}{1-0.8}
=3.3616
\]

每轮相对时间成本：

\[
1+\gamma c
=1+4\times0.05
=1.2
\]

所以：

\[
\text{Speedup}
=
\frac{3.3616}{1.2}
\approx2.80
\]

即理论上约为普通解码的 \(2.8\) 倍。

该定理依赖两个关键假设：

- 不同位置的候选接受事件近似独立，平均接受率为 \(\alpha\)；
- 目标模型对 \(\gamma+1\) 个位置的验证可以充分并行，walltime 近似为一次目标模型调用。

来源：:codex-file-citation{path="/home/hesai/推测解码/output/papers/01_Fast_Inference_from_Transformers_via_Speculative_Decoding.pdf" purpose="source"}
