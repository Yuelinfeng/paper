ffn_a = cat([state_a.to(bf16), message_a], dim=-1)
ffn_b = cat([state_b.to(bf16), message_b], dim=-1)

hidden_a = linear0(ffn_a)
hidden_b = linear0(ffn_b)


def fused_mixed_concat(state_a_fp32, message_a_bf16,
                       state_b_fp32, message_b_bf16):
    for side in [A, B]:
        out[side, :, :E] = bf16(state[side])   # 转 BF16
        out[side, :, E:] = message[side]       # 直接拷贝
    return out  # [2T, 2E]


| 融合位置 | 融合内容 | 作用 |
|---|---|---|
| F1：Attention 前处理 | `QKV` 拆分 + 对 Q/K 应用 RoPE + 直接写成 FlashAttention 所需的 `[B,H,T,D]` 布局 | 避免分别切分 Q/K/V、单独执行 RoPE、再做 layout 转换与连续化拷贝；减少中间 Tensor、显存读写和 kernel launch。 |
| CAT：FFN 输入准备 | A/B 两路的 FP32 state 转 BF16 + 各自与 BF16 attention message 拼接 + 写成 batch=2 的 `[2T, 2E]` FFN 输入 | 避免两次独立的类型转换与 `cat`；让后续 `Linear0` 可以一次处理 A/B 两路。Linear0 GEMM 本身仍是下一步独立执行。 |
| F2：FFN 非线性段 | `LayerNorm` 的 FP32 均值/方差统计 + 归一化 + affine（\(\gamma,\beta\)）+ 精确 GELU + 写回 BF16 | 不需要将 LayerNorm 输出、affine 输出、GELU 输出分别落到显存；保留关键计算的 FP32 稳定性，同时减少中间激活和同步。 |
| F3：FFN 残差段 | FP32 residual state + BF16 Linear3 projection → FP32 输出；A/B 两路在同一 kernel 中处理 | 避免为 A/B 分别发射 residual-add kernel；每线程以向量化方式读取 BF16 projection、转 FP32 后加到残差流。这里 Linear3 GEMM 不在同一自定义 kernel 内。 |
