# 组会汇报 PPT 内容草稿
## 任务漂移下的 MoE 专家缓存失效：现象确证与 FineMoE 上的漂移感知创新

> 受众：导师 + 同组研究生（熟悉深度学习，不一定熟悉 MoE offload 细节）
> 时长：约 15 分钟，共 18 页
> 格式：每页 ①标题 ②要点 ③讲稿备注 ④图/表资产

---

## Slide 1 · 题目页

**标题**
任务漂移下的 MoE 专家缓存失效：
现象确证与漂移感知 FineMoE 改造方案

**副标题**
批判性复现与系统扩展

**要点**
- 汇报人：[姓名]
- 日期：2026-08-28
- 方向：MoE 推理 / 专家 offload / 在线缓存策略

**讲稿备注**
今天汇报分两部分：第一部分是我们用真实 trace 做的现象确证实验——证明「任务漂移」确实存在，并且系统性地破坏频率缓存；第二部分是在此基础上提出的 FineMoE 改造方向。整个故事有一条实证驱动的主线，先把问题「说死」，再谈怎么解。

---

## Slide 2 · 背景：MoE 模型与专家 offload

**标题**
背景：MoE 模型为什么需要专家缓存

**要点**
- MoE（Mixture-of-Experts）每层有 N 个专家子网络，每次推理只激活其中 top-k 个（如 k=1 或 k=8）
- 大型 MoE 模型（DeepSeek-R1 / Llama4-Maverick / Kimi-K2）参数量达数百 GB，单张或少量 GPU 无法全部驻留显存
- 解法：**专家 offload**——GPU 只放小批专家（容量 C），其余放 CPU 内存或 SSD；token 需要哪个专家、就临时搬运（miss → 搬运，hit → 直接用）
- **命中率**越高，搬运次数越少，延迟越低——因此**缓存哪些专家是性能关键**
- **ρ = miss 延迟 / 计算延迟**（无量纲比值）：ρ 越大，一次 miss 的代价越贵

**讲稿备注**
先对齐基本概念。MoE 的核心思路是稀疏激活——每个 token 只走少数几个专家，其余不参与计算，所以推理算力可控。但代价是专家权重分散、单机显存装不下，offload 是目前工程上最主流的部署方式。这里引入 ρ 这个无量纲比值，后面 motivation 会直接用它：ρ 大意味着搬运一次专家的代价远超计算，缓存命中率的重要性被放大很多倍。

---

## Slide 3 · 被挑战的论文：《Patterns behind Chaos》

**标题**
被挑战的论文：关于 MoE 路由是否有规律可循

**要点**
- 论文《Patterns behind Chaos》（PbC）主张：MoE 专家路由**并非随机**，存在可利用的**全局集中性（G）和时间局部性（T）**
- 核心证据：热专家跨层跨模型稳定存在；近期访问具有重复性
- 该论文**未区分任务类型**——所有 benchmark 混合评测，默认路由分布在整个使用过程中「足够稳定」
- **我们的批判性切入点**：一旦请求流的任务类型发生切换，这个「稳定」假设还成立吗？

**讲稿备注**
PbC 的结论——路由有规律——我们并不整体否定，G 和 T 在单一任务内确实成立，我们自己的实验也复现了这两点。我们的切入点更精准：这篇论文的评测里，不同任务的 trace 是混在一起的，它没有问「如果任务变了会怎样」。这是它的盲区，也是我们的楔子。

---

## Slide 4 · 我们的核心发现：任务漂移

**标题**
核心发现：任务切换时专家集合几乎完全不重叠

**要点**
- **任务漂移（Task Drift）**：不同任务类型下，MoE 各层激活的专家集合随时间发生非平稳迁移
- 三个真实来源：① 多租户 serving（代码助手 + 数学 + 问答混流）；② Agentic 流水线（同一会话内跨任务跳转）；③ 长会话主题漂移
- 本文**主评测场景**：顺序漂移（任务 A → 任务 B 的顺序切换），对应来源②③；多租户为 stretch
- 核心量化工具：**Jaccard 相似度** = |A 热专家集 ∩ B 热专家集| / |A 热专家集 ∪ B 热专家集|，衡量两段请求共享多少热专家

**讲稿备注**
任务漂移不是我们人为构造的假设，而是数据里就存在的现象——下一页会直接展示。Jaccard 是最直观的集合相似度：两个集合完全一样=1，完全不重叠=0。我们测的是「做代码题时最常用的专家」和「做考试题时最常用的专家」有多大交集。

---

## Slide 5 · 证据一：分布层 Jaccard——漂移是硬事实

**标题**
证据一：跨任务热专家 Jaccard ≈ 0.07，同任务基线 0.51–0.81

**要点**
- 实验设置：取 livecodebench（代码题）和 mmlu（考试题）的 trace，各取 3 段互不相交子集（n=100/段），测跨任务 vs 同任务的 Jaccard
- **跨任务 cross：Jaccard @ top-4 ≈ 0.073**；同任务 within-lcb ≈ 0.722，within-mmlu ≈ 0.509
- 跨任务降幅约 **85–90%**：约 95% 的 livecodebench 热专家在 mmlu 中不是热专家
- 在 48 个跨 benchmark 配对中，40 个的 CI 上界 < 0.3，无一触及 0.5
- 注：频率向量余弦（0.930）仍高——**频率向量看不出差异，集合交集才看得出**，说明细粒度集合指标的必要性

**讲稿备注**
这是整个论文最硬的一块地基，完全独立于缓存仿真，只是在问「做代码题时最活跃的那几个专家，和做考试题时最活跃的那几个专家，是不是同一批人」——答案是几乎完全不是同一批。顺带说一句，余弦相似度没有发现这个差异，但集合 Jaccard 发现了；这也说明了为什么我们要用集合而不是向量度量。

**图表资产**：`analysis_out/plots/jaccard_DeepSeek.svg`（或 `jaccard_Llama4.svg`）；可制作一个简单表格呈现 cross/within 对比数字。

---

## Slide 6 · 证据一（续）：多模型、多 benchmark 泛化性

**标题**
跨任务漂移在 5 个模型、多 benchmark 上一致成立

**要点**
- 测试覆盖：DeepSeek-R1 / Llama4-Maverick / Kimi-K2 / Qwen3-235B（已发布 trace）+ Qwen1.5-MoE（本文采集，top-4）
- top-k 跨 k∈{1, 4, 8}，模型参数量跨一个数量级
- 全局集中性（G）和时间局部性（T）在**单任务内**通过全部 16 个测试——即 PbC 的观察在单任务内成立
- 但**跨任务 S 失败**：48/48 跨 benchmark cell 均低重叠，40/48 CI 上界 < 0.3
- 结论：**G 和 T 是单任务内的规律；漂移是这个规律的边界**

**讲稿备注**
这里做一个清晰的区分：PbC 说的"有规律"在单一任务内我们也复现了，不是它错了。我们证明的是它这个结论适用的边界——一旦换任务，专家集合的稳定性就消失了。我们把这个边界量化出来，这才是我们对这篇论文的批判性贡献。

**图表资产**：`analysis_out/plots/A3_crossmodel_global.svg`；可配跨任务 S 失败的 CI 汇总表。

---

## Slide 7 · 为什么漂移对 offload 是致命的

**标题**
为什么漂移专门打 offload 的命脉？

**要点**
- offload 的价值全部依赖「缓存预测对了」：命中 → 快；miss → 搬运，代价是 PCIe 或 SSD 传输
- **真机实测 ρ（A100-80GB-PCIe）**：
  - Qwen3-235B（36MB/专家）：ρ = 22–50
  - DeepSeek-R1（84MB/专家）：ρ = 52–58
  - Llama4-Maverick（240MB/专家，top-1）：ρ = **65–98**
  - SSD/NVMe offload（O_DIRECT 冷读）：ρ = **208–312**
- ρ 全部落在仿真饱和平台（≥25）——**延迟倍数对 ρ 精确值不敏感，只增不减**
- 对比：GH200 相干链路 ρ ≈ 1（缓存仅值 1.10×）；offload 下缓存价值被放大十数倍

**讲稿备注**
ρ 可以理解为「搬错一个专家有多贵」。ρ=65 意味着一次专家缺失的等待时间是它计算时间的 65 倍。在这种代价比下，缓存如果选错了，代价立刻被放大。SSD 更极端：ρ 可以到 300，相当于搬错一次要等 300 个计算周期。常驻显存的机器上缓存几乎无所谓，但 offload 场景里缓存策略是性能命脉。

**图表资产**：`analysis_out/motiv_drift_latency.svg`（两联图 Fig.1 (b)，纵轴延迟倍数 vs ρ + 真机操作点菱形 + GH200/offload 硬件区间带）

---

## Slide 8 · Motivation：漂移使错误缓存代价放大 1.33–2.05×

**标题**
Motivation：漂移下"选错缓存策略"的端到端延迟倍数

**要点**
- 场景：A(livecodebench) → B(mmlu) 顺序漂移，只在 B 段统计；4 个模型；per-query bootstrap 95% CI
- **代价模型**：延迟比 = (1 + miss率_策略 × ρ) / (1 + miss率_LRU × ρ)，ρ 大时饱和于 miss 率之比
- 两个口径（必须分开读）：
  - **冻结热表**（检索命中旧任务的悲观代理）：1.33×–2.05×，Llama4 top-1 最严重
  - **在线 LFU 毒化**（被陈旧频率污染的在线实测）：0.98×–1.49×，方向一致
- CI 极窄（±0.03×）；ρ=25→100 增量 ≤0.03×——**数字对 ρ 取值不敏感，已进入饱和平台**
- 额外证据（α 扫描）：即便一半缓存槽来自 B 的真实热专家，延迟仍 1.88×——**静态缓存结构上就抓不住时序局部性**

**讲稿备注**
这张图直接回答「任务漂移值多少钱」。1.33–2.05 这个范围是怎么来的：我们用实测的 miss 率差和真机 ρ，通过延迟代价模型算出来的，不是猜的。两个口径分开报是因为诚实——"检索命中错任务"是最坏情况代理，"在线 LFU 被毒化"是实际运行时发生的事。α 扫描最锋利：即使一半缓存内容已经是"正确答案"，延迟还是接近 1.88×，说明静态缓存的结构性缺陷不是"填对几个"就能解决的，必须有在线更新的时序机制。

**图表资产**：`analysis_out/motiv_drift_latency.svg` Fig.1 (a)（miss 率柱状 + CI）+ Fig.1 (b)（延迟倍数 vs ρ）；`motiv_drift_latency.json` 表格数据。

---

## Slide 9 · 证据二：LFU 被陈旧频率毒化，LRU 近免疫

**标题**
证据二：缓存命中率——LFU 被任务A毒化，LRU 近免疫

**要点**
- **EXP-B 实验**：用任务 A（代码题）预热缓存 → 切到任务 B（考试题）测命中率
  - LRU：进入 B 后，A 的专家因「最久没用」自然被冲掉 → warm ≈ cold（差 < 0.05 pp）
  - LFU：A 的陈旧高频计数把用不上的 A 专家「钉」在缓存里 → LFU_warm < LFU_cold
- **漂移分量 D = (LRU−LFU)_warm − (LRU−LFU)_cold**（D>0 = 漂移专惩罚频率缓存）
- Qwen1.5-MoE（top-4）多样 57 科 mmlu（dense 4560 条，NBOOT=1000）：

  | 容量 C | D 点估 | 95% CI | 显著? |
  |-------|--------|--------|-------|
  | 8 (13%N) | +0.96 pp | [−0.23, +1.11] | ❌ CI 跨 0 |
  | 12 (20%N) | +1.79 pp | [−0.36, +1.74] | ❌ CI 跨 0 |
  | 16 (27%N) | +2.39 pp | [−0.45, +2.41] | ❌ CI 跨 0 |

- ⚠ **诚实标注**：D 方向正、随容量单调、机制自洽；但多样 mmlu 下无任一容量达 per-C 显著（仅 3 科同质任务下曾显著）

**讲稿备注**
先解释 D：把四个命中率做一个"差中差"，目的是把"任务切换专门对频率缓存的额外伤害"隔离出来。数字上，方向一直是正的、随容量越来越大，这说明效应是真实存在的。但我们明确说：在标准的多样 mmlu 下，样本量还不足以把这个 1~2 个百分点的效应从噪声里拎出来达到 per-C 统计显著。我们不夸大这个结果——它是佐证，主地基是上面那张 Jaccard 表。

**图表资产**：`analysis_out/plots/exp4_hitrate_DeepSeek.svg`（或 Llama4.svg，命中率随容量曲线）；可制作 D-CI 表格插入。

---

## Slide 10 · 证据三：5 模型跨粒度对齐

**标题**
证据三：5 个模型方向一致——top-k 是关键调节变量

**要点**
- 5 个模型（k∈{1,4,8}，参数量跨一个数量级）做同口径 EXP-B（warm@lcb → eval@mmlu 3科150条，event-atomic）
  
  | 模型 | top-k | C>k? | 漂移分量 D |
  |------|-------|------|-----------|
  | Llama4-Maverick | 1 | ✅ | **+7.77 pp** 稳健 |
  | Kimi-K2 | 8 | ✅ | +2.18 pp |
  | DeepSeek-R1 | 8 | ✅ | +1.27 pp |
  | Qwen1.5-MoE | 4 | ❌（forced） | ≈ 0（容量伪影） |
  | Qwen3-235B | 8 | ❌（forced） | ≈ 0（容量伪影） |

- **核心规律**：C > top-k（缓存能跨步保留）时 D>0 且方向一致；C ≤ top-k（forced，每步全换）时 D≈0——这是容量约定伪影，非"大模型偏好频率"
- **k=4 补点**（Qwen1.5-MoE，C>k 容量扫描，NBOOT=200）：CI 下界 +0.90→+7.31 pp（随 C 单调），k∈{1,4,8} 三点曲线闭合

**讲稿备注**
"forced 区"（C ≤ top-k）是一个陷阱：缓存每步都被完全替换，预热状态根本留不住，所以 D≈0 不是因为漂移不存在，而是容量设得太小、漂移根本没机会留痕。论文里常用 5% 容量惯例，一些大模型的 top-8 在这个容量下刚好落进 forced 区，就会得到"漂移不影响"的错误结论。我们把这个伪影显式标出来，是方法论上的一个贡献。top-1 模型（Llama4）D 最大这一点不是偶然——top-1 单专家承载全部计算，一次 miss 最贵，漂移代价也最严重，物理上是自洽的。

**图表资产**：`analysis_out/outputs/expb_k4_capsweep.json` 的 D-CI 数据；可制作三点（k=1/4/8）折线图或表格。

---

## Slide 11 · 现有系统分析：FineMoE 的隐含假设

**标题**
现有 SOTA：FineMoE（EuroSys'26）的机制与漂移下的失效

**要点**
- FineMoE 是目前 MoE offload 的 SOTA（−47% 延迟 / +39% hit rate），核心机制：
  - **Expert Map Store**：记录历史每个 token 的逐层专家路由概率分布
  - **语义 + 轨迹相似度检索**：新 prompt 来时找历史中最相似的 map，按它预取
  - **LFU 变体驱逐**：频率缓存（论文明确拒绝了 LRU）
- **隐含假设**：「历史任务身份 = 对当前有用的信息」
- **漂移下的三个失效点**：
  1. 相似度塌陷 → 检索命中旧任务 map → **预取大面积误判**
  2. LFU 陈旧计数 → 旧任务专家赖着不走 → **缓存污染**
  3. Store 管理无时序概念 → 旧任务 map 占坑 / 新任务冷启 → **预取缺代表**
- **FineMoE 从未把任务漂移作为评测轴**，Store 离线预填满后评测，无在线切换场景

**讲稿备注**
FineMoE 不是靶子——它在它评测的场景里做到了 SOTA，我们要复用它的基础设施。我们的角色是「把它没测的场景测出来，并在它没有的地方加一层」。FineMoE 有一个非常精妙的地方：它在计算相似度预取时已经算出了一个相似度分数，但这个分数它只用来调节预取阈值，没有用来做漂移检测——我们下一页会说这个分数就是我们的零成本漂移信号。

---

## Slide 12 · 科学张力：同一 LFU-vs-LRU 问题，两个研究的分歧

**标题**
可证伪的科学张力：LFU vs LRU——分布内 vs 漂移下

**要点**
- **FineMoE 的消融（Fig 14b）**：在分布内（同任务），LFU > LRU → 它选 LFU、主动弃用 LRU
- **我们的 EXP-B（已验证）**：漂移下 LFU > LRU 反转/缩小；top-1 Llama4 LRU 胜 +20.4 pp（稳健）；k=4 C>k 全区 LRU 显著胜（+0.90→+7.31 pp 单调）
- **任务漂移正是两个结论分岔的 regime**：
  - 准平稳负载下：LFU 正确（频率统计稳定可信）
  - 漂移下：LFU 反噬（频率统计变负债）
- **论文核**：最优缓存策略随 regime 切换——而 FineMoE 没测那个 regime，也没有切换机制

**讲稿备注**
这是整个工作最重要的一张幻灯片，定义了我们论文的科学贡献所在。我们不是说 FineMoE 错了，而是说"最优策略随场景变化"——这是一个比"A 比 B 好"更深刻的结论。FineMoE 在准平稳负载下选 LFU 是完全正确的，在那个 regime 里它就应该用 LFU。我们的贡献是证明了另一个 regime 的存在，并提供了切换机制。

---

## Slide 13 · 创新方向：漂移感知控制器（DAC）概览

**标题**
创新方向：漂移感知控制器（DAC）——在 FineMoE 上加一层元决策

**要点**
- **核心 idea**：FineMoE 每次推理已经计算了相似度分数 `score`（语义 + 轨迹），但只用来设预取阈值。我们把它扩展为**在线漂移信号** `d = 1 − score`
- **零计算额外开销**：复用已算出的 `score`，只做标量重加权（微秒级）；recency 时间戳字段已存在于 C++ 基座（`last_access_time`），CP1 连内存都不新增
- **三个控制点（随 d 连续切换，非硬切换）**：
  1. **Recency 混合驱逐**：d 低→退回 LFU；d 高→偏 LRU；rank 归一化混合，无量纲问题
  2. **Prefetch/bypass 预算门控**：d 高 + 带宽紧 → 停止按 stale-map 预取，退到 on-demand
  3. **Store 时效刷新**：持续高 d 超 W 步窗口 → 加速写入当前任务 map，逐出陈旧任务条目
- 不是「换成 LRU」，是随漂移度连续滑动的元控制器

**讲稿备注**
「零开销」需要解释清楚：这里的零开销是指零计算额外开销，不是零内存。但由于 FineMoE 的 C++ 基座里已经有 `last_access_time` 字段并持续维护，连内存都基本不用增加——只是把这个字段接进驱逐排序键。代码里甚至有一段被注释掉的旧 LRU 驱逐代码，说明 FineMoE 自己曾经有过 LRU 路径，然后主动换掉了——我们的 CP1 相当于在漂移条件下有条件地把这条被弃用的 recency 信号重新接回来。

---

## Slide 14 · DAC 的三个控制点详解

**标题**
三个控制点：各自解决一个失效机制

**要点**
- **CP1 — Recency 混合驱逐**（对应缓存污染）
  - 按 rank 归一化：分别对 LFU 键和 recency 时间戳排分位，再用 d 加权混合
  - 分布内（d→0）完全退回 FineMoE LFU；漂移时（d→1）完全走 LRU
  - 注入点：`task_scheduler.cpp:296`（C++，`last_access_time` 字段已在）
- **CP2 — Prefetch/bypass 预算门控**（对应预取误判）
  - FineMoE 低相似度时「预取更多」对冲；我们主张：低相似度 + 带宽预算紧 → 预取越多越是烧带宽买坏预测
  - d 高 + 预算紧 → bypass，退到 on-demand + recency 缓存兜底
  - 注入点：`model_offload.py:237–252`（Python，易改）
- **CP3 — Store 时效刷新**（对应 map 缺代表）
  - 持续高 d（连续 W 步）才触发，防止单个离群 prompt 污染 Store
  - 加速 admit 当前任务轨迹为新 map；驱逐键加 staleness 权重让旧任务 map 优先出
  - 注入点：`model_offload.py:121–158`（Python，易改）

**讲稿备注**
CP2 和 FineMoE 的分歧是最本质的：FineMoE 认为越不确定越应该多预取来对冲，我们认为越不确定 + 带宽越贵，预取的期望价值越低，不如不预取。这个争议的裁判是实验——M1 的特征化实验就是要测出 δ 对冲在漂移+紧预算下是否真的无效或有害。CP3 的「窗口 W」设计是为了区分「真正的任务切换」（持续低相似度）和「偶发的离群 prompt」（短暂低相似度），防止把异常请求的路由模式写进 Store 污染未来检索。

---

## Slide 15 · 新颖性边界与相关工作

**标题**
我们的工作站在哪里：新颖性边界

**要点**
- **FineMoE（EuroSys'26）**：强基线，我们复用其 map/相似度/异步基建，替换决策层
- **SwapMoE（ACL'24）**：移动端，假设局部性不变——我们攻击的恰是「局部性失效」的 regime
- **DAOP（DATE'25）**：仅每序列内自适应，不处理跨任务；无 SSD；top-2 only
- **ProMoE（2410）**：离线固定预测器，零在线自适应；无漂移讨论；无 SSD
- **三个竞品共同没有的白地（✅ 均已核查）**：
  1. 无任何竞品处理 task drift / 多租户 / agentic 场景
  2. 无任何竞品有 SSD 层实测
  3. 无任何竞品用相似度分数做在线漂移信号切换策略
- **我们的新颖性 = (零开销漂移信号) × (元控制器切换逻辑) × (SSD/PCIe 带宽预算门控) × (top-1 覆盖)**

**讲稿备注**
新颖性不来自某一个单独的策略——recency 驱逐、激进预取这些单极策略都有先例。我们的贡献是「用什么信号、在什么条件下、在哪些极点之间切换」这一层元决策，以及把这一层嫁接在 FineMoE 已有的基础设施上、做到近零开销。SwapMoE 最像潜在竞争者，但它建立在「局部性恒定」的假设上——而我们的整个工作恰恰是在攻击这个假设被破坏时的情况。

---

## Slide 16 · 实验设计与路线图

**标题**
实验计划：从诊断到修复的四步路线图

**要点**
- **M0（✅ 已完成）**：等容量 bug 修正（hybrid 严格 C 容量重跑）+ k=4 trace 采集 + EXP-B 容量扫描；k∈{1,4,8} 三点曲线闭合
- **M1（本地 harness ✅ ready，等远程 GPU）**：漂移注入协议 + 测 vanilla FineMoE 预取精度/命中率随漂移塌陷；证 δ 对冲失效
- **M2（待实现）**：DAC 三控制点实现 + trace 级消融矩阵（5 配置：FineMoE / DAC-1only / DAC-2only / DAC-12 / DAC-full）+ 分布内不劣验证
- **M3（后续）**：真机集成（FineMoE 仓库 + SSD 层 + top-1 模型接入）+ 端到端延迟主结果
- 关键方法论约束：等容量 / event-atomic / forced-admission 分解 + Belady 天花板 / bootstrap CI

**讲稿备注**
M0 是整个方向的「低风险验证」——我们在做 DAC 之前先把动机数据的方法论质量保证好。等容量 bug 这个细节值得一提：原始实验里 hybrid 缓存用了 1.5C 的容量，相当于作弊，修正后结论更保守、也更诚实。M1 是最关键的实验：如果 δ 对冲在漂移+紧预算下真的有害，DAC 的 CP2 就有了实验铁证支撑；如果不有害，我们需要重新审视 CP2 的定位。

---

## Slide 17 · 诚实限制与开放问题

**标题**
诚实限制与开放问题

**要点**
- **缓存 D 在多样 mmlu 下未达 per-C 显著**：效应真实但幅度小（1~2 pp），多任务噪声把功效稀释到 CI 跨 0；不拿它 claim 统计显著
- **「随任务连贯度缩放」已撤回**：密集补采后 COHERENT-12 ≈ DIVERSE-12，这条路径放弃
- **DAC 尚无 M1–M3 实验结果**：目前是方案 + 动机证据；「漂移感知能赢 FineMoE」是假设，还需实验验证
- **并发多租户是 stretch**：主评测是顺序漂移；多租户需要跨请求信号聚合 + 补并发调度层
- **top-1 真机是后期目标**：M1/M2 用 trace 级模拟，真机 top-1 集成在 M3（工作量大）

**讲稿备注**
这页很重要——我们在汇报里主动把结论的边界说清楚，比被老师问到再解释要好得多。特别是缓存 D 的显著性：我们有点估计、有方向、有机制，但没有 per-C 的统计显著，这个差距要诚实交代。DAC 部分目前是「有充分动机、有代码级落地方案、但没有实验结果」，汇报里要区分「已验证」和「待验证」。

---

## Slide 18 · 小结与下一步

**标题**
小结：我们做了什么，下一步做什么

**要点**
**已确立（实验验证）**
- 跨任务热专家 Jaccard ≈ 0.07（分布层铁证，独立于缓存仿真）
- 漂移系统性惩罚 LFU、LRU 近免疫——5 模型方向一致，k∈{1,4,8} 三点闭合
- 真机实测 ρ = 22–312，全落饱和平台；延迟倍数 1.33–2.05×（bootstrap CI ±0.03×）
- FineMoE 的 LFU vs LRU 分歧：分布内 LFU 正确，漂移下 LRU 更优——最优策略随 regime 切换

**已设计，待验证**
- DAC 三控制点方案；代码 4 个注入点已定位；k=4 G0-a 判据压倒性通过
- M1 harness ready，等远程 GPU sweep

**下一步行动**
- 近期：M1 远程 sweep（用户侧执行）→ 证 δ 对冲失效，完成「问题成立」的实证
- 中期：M2 DAC 实现 + 消融 → 完成「解法有效」的证明
- 远期：M3 真机集成 + SSD + top-1 → 端到端延迟主结果

**讲稿备注**
用一句话收尾：我们在一个 FineMoE 没有测过的 regime 里，用 FineMoE 自己已经算出的信号，来修复 FineMoE 在那个 regime 下的失效——这是增量而非替代，贡献是把「最优策略随任务分布变化」这一层显式做出来。

---

## 附：图表资产索引

| Slide | 使用资产 | 路径 |
|-------|---------|------|
| 5 | 跨任务 Jaccard 散点/热图 | `analysis_out/plots/jaccard_DeepSeek.svg` 或 `jaccard_Llama4.svg` |
| 6 | 跨模型全局集中性汇总 | `analysis_out/plots/A3_crossmodel_global.svg` |
| 7 | 延迟倍数 vs ρ + 真机操作点 | `analysis_out/motiv_drift_latency.svg`（Fig.1 右联） |
| 8 | miss 率柱状 + CI + 延迟倍数 | `analysis_out/motiv_drift_latency.svg`（两联图） |
| 9 | 命中率随容量曲线（LRU/LFU warm/cold） | `analysis_out/plots/exp4_hitrate_DeepSeek.svg` |
| 10 | k=4 容量扫描 D-CI 折线 | 由 `analysis_out/outputs/expb_k4_capsweep.json` 生成 |
| 16 | 路线图（文字/表格，无需图） | — |

